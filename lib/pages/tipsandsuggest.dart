import 'package:flutter/material.dart';

class Tipsandsuggest extends StatelessWidget {
  const Tipsandsuggest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Tips goes here',
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.w700),
      ),
    );
  }
}
